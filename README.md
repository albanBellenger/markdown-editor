# Collaborative Markdown Workspace Demo

Small React app wrapper around `CollaborativeMarkdownWorkspace` so you can run and test it quickly.

## Run locally

```bash
npm install
npm run dev
```

Then open the local URL printed by Vite (usually `http://localhost:5173`).

## Build

```bash
npm run build
npm run preview
```
